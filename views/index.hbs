

<a class="add-link"  href=""><i class="fa fa-4x fa-plus-circle"></i></a>
{{> items }}
<div class="panel panel-black">
    <div class="panel-body">
        {{#monthlyTags}}
        <button data-id="{{_id}}" type="button" class="pay-link btn {{currentMonth lastDate 'btn-success' 'btn-danger'}}">
            {{_id}} 
        </button>
        {{/monthlyTags}}
    </div>
</div>
<script>
    initItemListCallbacks();
</script>